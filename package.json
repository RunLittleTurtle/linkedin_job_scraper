json
{
  "name": "linkedin-job-scraper",
  "version": "1.0.0",
  "description": "LinkedIn job scraper that extracts job listings and stores them in NocoDB",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "playwright": "^1.39.0",
    "axios": "^1.5.1",
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.2",
    "user-agents": "^1.0.1444",
    "p-limit": "^4.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
